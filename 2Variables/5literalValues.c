#include<stdio.h>

int main() {
  // ---------- LITERALS ----------
  int literal_int = 42;        // 42 is an integer literal
  float literal_float = 3.14f; // 3.14f is a float literal
  char literal_char = 'A';     // 'A' is a character literal
  char literal_str[] = "Hi";   // "Hi" is a string literal
  
  // ---------- NOT LITERALS ------
  /*
  Variables
    int x = 10;
    x is not a literal. It just holds a literal.

  Expressions
    a + b
    x * 2
    n / 3.14
    An expression computes a value; it’s not written as a raw constant.
  */

  return 0;
}